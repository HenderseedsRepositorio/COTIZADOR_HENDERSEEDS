# ── Archive (versiones viejas, pruebas, juegos) ──
archive/

# ── Subcarpetas legacy (contenido no trackeado que queda local) ──
1-Cotizador Semillas (Nidera)/
2-Cotizador Agronomia (Henderseeds)/
APP Seguimiento campaña/

# ── Claude / editor config ──
.claude/
*/.claude/
CLAUDE.md

# ── Archivos temporales / instrucciones sueltas ──
*.csv
*.xlsx
*.zip
*.txt

# ── Imagenes (excepto assets/) ──
*.png
!assets/*.png

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini
